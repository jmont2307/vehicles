{
  "name": "vehicle-builder",
  "version": "1.0.0",
  "description": "A CLI application for building and using different types of vehicles",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "nodemon --watch src --ext ts --exec \"npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.3.0",
    "inquirer": "8.2.4",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/inquirer": "^8.2.4",
    "@types/uuid": "^9.0.7",
    "nodemon": "^3.0.3",
    "typescript": "^5.0.0"
  }
}
